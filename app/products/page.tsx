import { ArrowUpRight } from "lucide-react";
import { getProducts } from "@/lib/db";
import { ArrowLink } from "@/components/arrow-link";

export default async function ProductsPage() { const products = await getProducts(); return <main><section className="page-hero"><div className="container"><div className="eyebrow">The DevTech AI product studio</div><h1>Products for a world that keeps changing.</h1><p>We build and back products where software, intelligence, and human behavior meet. Each one starts with a real need.</p></div></section><section className="section"><div className="container"><div className="product-list">{products.length ? products.map((product) => <article id={product.slug} className="product-detail" key={product.slug}><div className="visual" style={{background: product.color}}><div className="eyebrow">{product.eyebrow}</div><h2>{product.name}</h2></div><div className="details"><div><div className="product-status">{product.status}</div><p>{product.longDescription}</p></div><div><div className="detail-row"><span>Available on</span><span>{product.platform.join(" · ")}</span></div><div className="detail-row"><span>Built by</span><span>DevTech AI</span></div></div></div></article>) : <p style={{color: "var(--muted)"}}>Products will appear here once they are published in the admin workspace.</p>}</div></div></section><section className="statement"><div className="container"><div className="eyebrow" style={{color: "#9bc9b4"}}>A growing portfolio</div><h2>More products are taking shape in the studio. <span>We’ll share them when they’re ready.</span></h2><ArrowLink href="/contact">Work with us</ArrowLink></div></section></main>; }
