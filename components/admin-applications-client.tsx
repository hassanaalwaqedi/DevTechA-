"use client";
import { useEffect, useState } from "react";
import type { ApplicantStatus, Application } from "@/lib/data";

export function AdminApplicationsClient() { const [items, setItems] = useState<Application[]>([]); const [filter, setFilter] = useState("All statuses"); const [selected, setSelected] = useState<Application | null>(null); const [error, setError] = useState(""); async function load() { const response = await fetch(`/api/applications${filter !== "All statuses" ? `?status=${encodeURIComponent(filter)}` : ""}`, {cache: "no-store"}); const result = await response.json(); if (!response.ok) {setError(result.error || "Unable to load applications."); return;} setItems(result.applications || []); } useEffect(() => { load(); }, [filter]); async function update(app: Application, patch: Partial<Application>) { const response = await fetch(`/api/applications/${app.id}`, {method: "PATCH", headers: {"Content-Type": "application/json"}, body: JSON.stringify(patch)}); if (!response.ok) {const result = await response.json(); setError(result.error || "Unable to update application."); return;} await load(); setSelected({...app, ...patch}); } return <><div className="admin-toolbar"><div><select value={filter} onChange={(e) => setFilter(e.target.value)}><option>All statuses</option><option>new</option><option>reviewing</option><option>shortlisted</option><option>interview</option><option>accepted</option><option>rejected</option></select></div><span style={{color: "var(--muted)", fontSize: 13}}>{items.length} applications</span></div>{error && <p style={{color: "#a14e40", fontSize: 13}}>{error}</p>}<div className="admin-panel"><table className="admin-table"><thead><tr><th>Applicant</th><th>Position</th><th>Date</th><th>Experience</th><th>Status</th><th></th></tr></thead><tbody>{items.map((app) => <tr key={app.id}><td><strong>{app.name}</strong><br/><span style={{color: "var(--muted)", fontSize: 11}}>{app.country} · {app.email}</span></td><td>{app.jobTitle}</td><td>{app.createdAt}</td><td>{app.experience}</td><td><span className={`status ${app.status}`}>{app.status}</span></td><td><button className="button-small" onClick={() => setSelected(app)}>Open</button></td></tr>)}</tbody></table></div>{selected && <div className="admin-panel" style={{marginTop: 15}}><div className="admin-toolbar"><div><div className="eyebrow">Application {selected.id}</div><h2 style={{margin: "7px 0 0"}}>{selected.name}</h2></div><button className="button-small" onClick={() => setSelected(null)}>Close</button></div><div className="detail-row"><span>Position</span><span>{selected.jobTitle}</span></div><div className="detail-row"><span>Links</span><span>{selected.github || "No GitHub"} · {selected.linkedin || "No LinkedIn"}</span></div><div className="detail-row"><span>CV</span><a href={`/api/applications/${selected.id}/cv`} target="_blank" rel="noreferrer" style={{color: "var(--green)"}}>{selected.cvName} ↓</a></div><div className="field" style={{marginTop: 20}}><label>Status</label><select value={selected.status} onChange={(e) => update(selected, {status: e.target.value as ApplicantStatus})}><option>new</option><option>reviewing</option><option>shortlisted</option><option>interview</option><option>accepted</option><option>rejected</option></select></div><div className="field"><label>Admin notes</label><textarea defaultValue={selected.note || ""} onBlur={(e) => update(selected, {note: e.target.value})} placeholder="Add a note for your team..." /></div></div>}</>; }
